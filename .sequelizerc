const { resolve } = require('node:path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.cjs'), // <- com .cjs aqui
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
  'models-path': resolve(__dirname, 'src', 'app', 'models'),
};
